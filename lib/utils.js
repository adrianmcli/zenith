export const getPrivateKeysFromPassphrase = (passphrase) => {
  const privateKeys = [`temp1`];
  return privateKeys;
};

export const getPublicAddressesFromPrivateKeys = (privateKeys) => {
  const publicAddresses = [`temp2`];
  return publicAddresses;
};
